{
  "name": "create-key-pair",
  "private": true,
  "version": "0.1.0",
  "description": "AWS CDK custom resource to create a key pair",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild index.ts --outdir=dist --bundle --minify --sourcemap --platform=node --target=node22 --external:@aws-sdk/client-secrets-manager"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3",
    "@aws-sdk/client-secrets-manager": "^3.750.0"
  }
}
